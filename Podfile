# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'SampleReactNativeHost' do
  # Uncomment this line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for SampleReactNativeHost

  target 'SampleReactNativeHostTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SampleReactNativeHostUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  pod 'React', :path => 'ReactNativeSupport/node_modules/react-native', :subspecs => [
    'Core',
    'RCTText',
    'RCTWebSocket', # needed for debugging
  ]

end

